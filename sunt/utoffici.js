// A possible implementation of pathShape function
function pathShape(pathData) {
  // This function might process the path data in some way
  console.log("Processing path shape:", pathData);
}

// Using the constructed expression
eval(expr); 
// This would be equivalent to calling pathShape(param(clip.path))
// Output: "Processing path shape: M10 10 H 90 V 90 H 10 Z"
